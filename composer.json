{
    "require": {
        "enqueue/amqp-lib": "^0.10.1",
        "symfony/console": "^5.1"
    },
    "config": {
        "optimize-autoloader": true,
        "preferred-install": "dist",
        "sort-packages": true
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    }
}
